environment: dev
namespace: beaesthetic-dev
replicaCount: 1

envConfig:
  SCHEDULER_NAME: reminders-dev
  SCHEDULER_POLLING_INTERVAL: 30s
  SCHEDULER_PEEK_LEASE_TTL: 30s
  SCHEDULER_PEEK_BATCH_SIZE: "25"

resources:
  requests:
    memory: 100Mi
    cpu: 50m
  limits:
    memory: 128Mi
    cpu: 100m

