// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: pending_notifications.sql

package queries

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const findPendingNotification = `-- name: FindPendingNotification :one
SELECT correlation_key, agenda_event_id, notification_type
FROM pending_notifications
WHERE correlation_key = $1
`

type FindPendingNotificationRow struct {
	CorrelationKey   string `json:"correlation_key"`
	AgendaEventID    string `json:"agenda_event_id"`
	NotificationType string `json:"notification_type"`
}

func (q *Queries) FindPendingNotification(ctx context.Context, correlationKey string) (FindPendingNotificationRow, error) {
	row := q.db.QueryRow(ctx, findPendingNotification, correlationKey)
	var i FindPendingNotificationRow
	err := row.Scan(&i.CorrelationKey, &i.AgendaEventID, &i.NotificationType)
	return i, err
}

const removePendingNotification = `-- name: RemovePendingNotification :exec
DELETE FROM pending_notifications
WHERE correlation_key = $1
`

func (q *Queries) RemovePendingNotification(ctx context.Context, correlationKey string) error {
	_, err := q.db.Exec(ctx, removePendingNotification, correlationKey)
	return err
}

const savePendingNotification = `-- name: SavePendingNotification :exec
INSERT INTO pending_notifications (correlation_key, agenda_event_id, notification_type, expires_at)
VALUES ($1, $2, $3, $4)
ON CONFLICT (correlation_key) DO UPDATE SET
    agenda_event_id = $2,
    notification_type = $3,
    expires_at = $4
`

type SavePendingNotificationParams struct {
	CorrelationKey   string             `json:"correlation_key"`
	AgendaEventID    string             `json:"agenda_event_id"`
	NotificationType string             `json:"notification_type"`
	ExpiresAt        pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) SavePendingNotification(ctx context.Context, arg SavePendingNotificationParams) error {
	_, err := q.db.Exec(ctx, savePendingNotification,
		arg.CorrelationKey,
		arg.AgendaEventID,
		arg.NotificationType,
		arg.ExpiresAt,
	)
	return err
}
